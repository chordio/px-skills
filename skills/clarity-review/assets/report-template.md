# Clarity review: <piece title or path>

- **Source:** `<path or "pasted draft">`
- **Reviewed by:** <agent / host>, <date YYYY-MM-DD>
- **Panel:** <e.g. "3× small/fast skimmers + 1× strong careful reader, per level"> · <`isolated` | `degraded (sequential)`>
- **Verdict:** <SHIP | FIX FIRST | REWORK OPENING> — <one line>

## Headline

<2–4 sentences: the confirmed L0 defects first (they cost the most readers), then
the most important deeper ones. If clean, say so plainly.>

## Per-entity resolution

| Entity | Appears @ | Resolves @ | Panel | Class | Note |
|---|---|---|---|---|---|
| `the map` | L0 | L2 | 4/5 NOT ESTABLISHED | `resolves-too-late` | readers can't tell map *of what* from the dek |
| `good benchmarks` | L0 | n/a | — | `unearned-claim` | body argues coverage, never quality |
| `<coined term>` | L1 | L1 | 5/5 RESOLVABLE | — | clean |

> `Panel` = agreement across the blind readers at the entity's appearance level.
> Majority `NOT ESTABLISHED` is a confirmed defect; a lone stumble is `watch`.

## "What is this about?" (panel, at L0 and L1)

- **L0 (dek only):** <range of reader answers — do they converge on the real thesis, or scatter?>
- **L1 (+intro):** <same>

> If readers can't state the contribution from L0/L1, that's a `contribution-unclear`
> blocker regardless of the entity table.

## Dek / title audit

- **D-1 contribution:** <pass/fail + why>
- **D-2 earned claims:** <list each adjective/claim → developed in body? y/n>
- **D-3 standalone parse:** <pass/fail + any unidentifiable reference>

## Given-new dangling referents (informed linear read of the body)

| Location (quote) | Dangling reference | Class | Severity |
|---|---|---|---|
| "…every result sounds like it competes with the last." | "the disagreement" never set up | `unmotivated-move` | blocker |

## Prioritized defects + suggested fixes

> Diagnosis only. Each fix is a proposal for the author, not an applied edit.

1. **[blocker] `<class>` — <entity/location>.** <what breaks> → *Suggested:* <fix>.
2. **[fix] …**
3. **[watch] …**

## Recurring-rule candidate

<If a defect class shows up across multiple pieces, note it as a candidate addition
to your house style guide / writing-principles doc (most such docs are subtractive
and silent on clarity). Otherwise "none this pass".>
